---
title:
  en: Enter data the German way
  de: Daten auf deutsche Art eingeben
case: wurzel
businessGoal:
  en: Accept German input conventions — a decimal comma for prices, tidy capitalization of names and text — regardless of the device locale.
  de: Deutsche Eingabekonventionen akzeptieren — Dezimalkomma bei Preisen, saubere Groß-/Kleinschreibung von Namen und Text — unabhängig vom Geräte-Locale.
priority: should
status: done
aiContribution:
  en: The AI traced the price-entry failure to the browser number input bound to the (English) OS locale and proposed a text input with app-side parsing; it also proposed the on-blur auto-capitalization. I confirmed the single-separator rule and which fields to exclude.
  de: Die KI führte das Preis-Eingabeproblem auf das an das (englische) OS-Locale gebundene Zahlen-Eingabefeld zurück und schlug ein Text-Eingabefeld mit App-seitigem Parsen vor; sie schlug auch die Auto-Groß-/Kleinschreibung beim Verlassen des Feldes vor. Ich bestätigte die Ein-Trennzeichen-Regel und die auszuschließenden Felder.
---
